## 1. 架构设计

```mermaid
graph TD
    A["前端 React 应用"] --> B["本地状态管理 (React Context)"]
    A --> C["数据持久化 (LocalStorage)"]
    B --> D["订单模块"]
    B --> E["价格配置模块"]
    D --> F["客户信息"]
    D --> G["衣物信息"]
    D --> H["尺寸测量"]
    D --> I["修改项目"]
    D --> J["瑕疵记录"]
    E --> K["修改类型单价"]
```

## 2. 技术描述
- 前端：React@18 + tailwindcss@3 + vite
- 初始化工具：vite-init
- 后端：无（纯前端 SPA，LocalStorage 持久化）
- 数据库：LocalStorage 存储 JSON 数据，含 Mock 初始数据
- UI 组件库：自定义组件（Tailwind 构建）
- 图标：lucide-react

## 3. 路由定义
| 路由 | 用途 |
|------|------|
| / | 订单列表页（首页） |
| /orders/new | 新建订单页 |
| /orders/:id | 订单详情/编辑页 |
| /pickup | 待取件列表 |
| /pricing | 价格配置页 |

## 4. 数据模型

### 4.1 数据模型定义

```mermaid
erDiagram
    ORDER ||--|{ MEASUREMENT : "包含"
    ORDER ||--|{ ALTERATION_ITEM : "包含"
    ORDER ||--o| DEFECT : "包含"
    PRICING_CONFIG {
        string id PK
        string name
        number price
    }

    ORDER {
        string id PK
        string orderNo
        string customerName
        string customerPhone
        string pickupDate
        string clothingCategory
        string fabric
        string brand
        string color
        string requirements
        string status
        number totalPrice
        string defectDescription
        boolean defectConfirmed
        boolean notified
        boolean pickedUp
        string createdAt
        string updatedAt
    }

    MEASUREMENT {
        string id PK
        string orderId FK
        string partName
        number originalSize
        number targetSize
        string unit
    }

    ALTERATION_ITEM {
        string id PK
        string orderId FK
        string type
        string name
        number unitPrice
        number quantity
        number subtotal
    }
```

### 4.2 数据结构定义

**订单 (Order)**
```typescript
type OrderStatus = 'pending' | 'in_progress' | 'ready' | 'completed';

interface Order {
  id: string;
  orderNo: string;
  customerName: string;
  customerPhone: string;
  pickupDate: string;
  clothingCategory: string;
  fabric: string;
  brand: string;
  color: string;
  requirements: string;
  status: OrderStatus;
  totalPrice: number;
  measurements: Measurement[];
  alterationItems: AlterationItem[];
  defectDescription: string;
  defectConfirmed: boolean;
  notified: boolean;
  pickedUp: boolean;
  pickedUpAt?: string;
  createdAt: string;
  updatedAt: string;
}
```

**测量数据 (Measurement)**
```typescript
interface Measurement {
  id: string;
  partName: string;
  originalSize: number;
  targetSize: number;
  unit: string;
}
```

**修改项目 (AlterationItem)**
```typescript
interface AlterationItem {
  id: string;
  type: string;
  name: string;
  unitPrice: number;
  quantity: number;
  subtotal: number;
}
```

**价格配置 (PricingConfig)**
```typescript
interface PricingConfig {
  id: string;
  type: string;
  name: string;
  price: number;
}
```

### 4.3 初始 Mock 数据

修改类型默认配置：
- 改短：¥30
- 放码：¥50
- 收腰：¥40
- 换拉链：¥35
- 补洞：¥25
- 改领口：¥45

预置 3-5 条示例订单数据，覆盖不同状态。
